// Exercise 02: Interactive Navigation

// TODO: Select the toggle button and the nav menu using document.getElementById()
// const navToggle = ...;
// const navLinks = ...;

// TODO: Using console.log, confirm both selections actually worked before moving on —
// this is the fastest way to catch a typo'd id.


// TODO: Add a click event listener to the button.
// Inside the callback, toggle the 'open' class on the nav menu.
// navToggle.addEventListener('click', () => {
//   ...;
// });

// TODO (important, not optional): also close the menu when a link
// inside it is clicked. Without this, someone on mobile taps "About",
// the page jumps there, and the menu is still sitting open over the
// content. Loop over the links and remove 'open' on each click:
//
// document.querySelectorAll('#navLinks a').forEach((link) => {
//   link.addEventListener('click', () => {
//     navLinks.classList.remove('open');
//   });
// });
