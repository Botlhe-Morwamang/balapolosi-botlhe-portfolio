// Exercise 03: Dark Mode — add this to your existing script.js
// Also add the button itself to index.html, in the same <nav> as the hamburger:
// <button id="themeToggle" class="theme-toggle" aria-label="Switch theme">🌙</button>

// TODO: Select the theme toggle button
// const themeToggle = ...;

// TODO: Add a click event listener that toggles a 'dark-mode' class
// on document.body, AND swaps the icon so the button always shows
// what it will switch TO, not what mode you're currently in:
//
// themeToggle.addEventListener('click', () => {
//   document.body.classList.toggle('dark-mode');
//   themeToggle.textContent = document.body.classList.contains('dark-mode') ? '☀️' : '🌙';
// });